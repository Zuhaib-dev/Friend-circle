const load = document.querySelector('.loading-container')

      window.addEventListener
      ('DOMContentLoaded', () => {
        setTimeout(() => {
          load.style.display = 'none'
          
        }, 3000);
      })
      